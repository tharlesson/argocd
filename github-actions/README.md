# Helpers do GitHub Actions

Scripts auxiliares usados nos workflows para manter a lógica de promoção de imagem simples e determinística.

- `scripts/update_tag.sh`: atualiza a tag da imagem em um overlay específico.
- `scripts/promote_tag.sh`: copia a tag do overlay de origem para o overlay de destino.

